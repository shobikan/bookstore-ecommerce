spring:
  application:
    name: bookstore-ecommerce
  datasource:
    url: jdbc:mysql://localhost:3306/bookstore
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: shobikan0@gmail.com
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

stripe:
  secret-key: ${STRIPE_SECRET_KEY}


